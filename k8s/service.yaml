apiVersion: v1
kind: Service
metadata:
  name: resumeai-service
  namespace: default
  labels:
    app: resumeai
spec:
  type: ClusterIP
  selector:
    app: resumeai
  ports:
    - port: 80
      targetPort: 3000
      name: http
      protocol: TCP
