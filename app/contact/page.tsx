import React from "react";
import ContactClient from "./contact-client";

export const metadata = {
  title: "Contact Us | ResumeAI Pro",
  description: "Get in touch with the ResumeAI Pro support team. Submit a general query, feedback, billing question, or technical support ticket.",
};

export default function ContactPage() {
  return <ContactClient />;
}
